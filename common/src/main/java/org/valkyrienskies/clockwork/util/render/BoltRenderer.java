package org.valkyrienskies.clockwork.util.render;

import org.joml.Vector3dc;

import java.util.Random;

/**
 * A small utility for drawing cuboid-line 'lightning bolts' between two points.
 */
public class BoltRenderer {
    private final Random randomizer = new Random();

    public void renderBolt(Vector3dc start, Vector3dc end, int numSegments, double thickness) {

    }
}

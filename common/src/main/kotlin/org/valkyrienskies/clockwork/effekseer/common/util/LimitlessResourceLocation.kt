package org.valkyrienskies.clockwork.effekseer.common.util

import net.minecraft.resources.ResourceLocation

class LimitlessResourceLocation(namespace: String, path: String) :
    ResourceLocation(namespace, path)